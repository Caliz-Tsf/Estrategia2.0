# Sesion-061

> Rescatado de `memory/ESTADO-ACTUAL.md` en S147 (poda del estado). Texto verbatim del párrafo que vivía en el estado; esta sesión no tenía fichero propio.

- **Última tarea completada ANTERIOR (Sesion-061, 2026-06-28):** **MENTORES: fix muro anti-bot yt-dlp en lotes (commit `a960feb`).** El muro "Sign in to confirm you're not a bot" en lotes boxxocode era **rate-limiting transitorio** por descargas paralelas, NO bloqueo permanente. **Las cookies son contraproducentes:** hacen que yt-dlp descarte los clientes android y caiga a web/tv → exigen n-challenge (runtime JS/Deno no instalado) → "Only images are available". **Fix en los 3 scripts** (`process-channel`, `process-video-vision`, `process-video`): forzar `--extractor-args youtube:player_client=android_vr,android,ios` (evita n-challenge, da 1080p) + `--retries 10 --extractor-retries 5 --sleep-requests 1.5` (sobrevive rate-limiting paralelo); cookies degradadas a params OPCIONALES OFF por defecto, doc en `scripts/.secrets/README.md`; `.gitignore` cubre `scripts/.secrets/` + `*cookies*.txt`. Verificado E2E (1080p OK). `COMANDOS-boxxocode.md` NO necesitó cambios. **0 commits Pine, CORE INTACTO** (1517 líneas SHA `80fad14dd8d03758`). Memoria `[[boxxocode-vision-nvidia-no-claude-tokens]]` actualizada (sección S061). **SIGUIENTE: continuar MAP del 2022 Mentorship ICT — episodios 14–41** → luego MMP+OTE → muestra 2024 → REDUCE a `ficha-mentor.md`. Lo destilado vive en `Mentores/ict/knowledge/2022-mentorship.md`. Ver [[ict-mentor-madre-build]].
